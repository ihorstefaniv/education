//task1
function calcRectangleArea() {
    try {
    let width = Number(prompt("Введіть ширину прямокутника", ""));
    let height = Number(prompt("Введіть висоту прямокутника", ""));

    let square = width * height;
  console.log(square);
    } catch (error) {
        console.log("жопа");
    }
}

calcRectangleArea();

//task2
function checkAge() {
    let age = promt("Введіть Ваш вік", "");
    
}
